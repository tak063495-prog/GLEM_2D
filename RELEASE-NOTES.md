# GLEM Release Notes

## v1.1.0 — 2026-08-25

P0改善として、アプリケーション全体の日本語・英語対応と配布物の回帰検証を追加。

### 主な変更

- Windows表示言語に追従する「システム既定」と、英語・日本語の明示選択を追加
- UI、入力検証、例外、解析結果、グラフ、ファイルダイアログを英日両言語化
- HTMLレポートを英語・日本語で生成し、言語にかかわらず数値・日付形式を安定化
- `.glem` とCSVの言語非依存・ドット小数形式を回帰テストで保証
- 製品バージョンを `Directory.Build.props` の `1.1.0` に一元化
- Release ZIPを展開して `GLEM.exe --selftest` を実行するGitHub Actionsゲートを追加

### 回帰結果

- Releaseビルド: 成功（警告0・エラー0）
- 自動テスト: 100/100 パス（Windows日本語環境）
- 英日リソース: 165/165キー一致、重複なし

## v1.0.0 — 2026-08-23

初版リリース。L4 受入判定書（`docs/GLEM_L4受入判定書.md`）に基づき承認済み。

### 機能

**斜面安定解析（限界平衡法）**
- Fellenius法・Bishop簡化法・Janbu一般化条体法の切替
- 円弧滑動面の自動探索（中心/半径の範囲指定または既定範囲）
- Janbu 非円滑動面: 制御点編集 UI + λc 補正係数（公開文献の閉形式補正式、D-04）
- 間隙水圧: ru 比（優先）または地下水位線に基づく静水圧
- 載荷 q [kPa]・地震係数 kh/kv の考慮
- 結果: FS_min、臨界滑動面、条体別内訳テーブル、断面図プロット（ズーム/パン対応）

**沈下予測（一次元圧密理論）**
- 即時沈下（Es）・一次圧密（Cc/e0/σpc'・U-Tv）・二次圧縮（Cs）の内訳表示
- 単重/二重排水の選択、期間・出力点数の指定
- S-t 曲線プロット（線形/対数時間軸切替）、T50/T90 到達時刻

**共通**
- プロジェクトファイル `.glem`（JSON、バージョン管理、メジャー不一致時の確認ダイアログ R-3.1.5）
- 自動保存と復元プロンプト（R-3.1.4）
- CSV エクスポート（UTF-8）・HTML レポート生成（図面埋め込み・印刷対応）
- 入力検証: セルハイライト + 仕様書準拠のエラーメッセージ（GLEM-1xxx/2xxx/3xxx）

### 品質指標

| 項目 | 結果 |
|---|---|
| テスト | 59/59 パス（xUnit、Release） |
| GLEM.Core カバレッジ | 94.09%（閾値 80%） |
| 標準モデル探索性能 | 7.79 s（基準 60 s 以内） |
| 沈下解析性能 | 0.012 s（基準 30 s 以内） |
| 起動時間 | メイン画面表示まで 2.4 s（基準 10 s 以内、クリーン環境実測） |

### インストール方法

`dist/GLEM-1.0.0-win-x64/` フォルダを任意の場所に配置し `GLEM.exe` を起動するだけ（自己完結型 .NET 8 ランタイム同梱のため、ランタイムの事前インストールは不要）。`.glem` ファイルをダブルクリックすると関連付けられた GLEM が起動してプロジェクトを開く。

### 既知の制限事項

- 対応 OS: Windows x64 のみ（WPF）
- 沈下解析は一次元圧密理論に基づく（側方流出・多層非等方透水は対象外）
- Janbu λc は公開文献の閉形式補正式による近似値

### 関連文書

- `docs/GLEM_ユーザーマニュアル.md` — 操作手順・FAQ
- `docs/GLEM_L4受入判定書.md` — 受入基準 Q-01〜Q-04 の検証記録
- `docs/perf/README.md` + `perf-log.jsonl` — 性能測定記録
